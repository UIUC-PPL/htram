module tramNonSmp {

  template <typename T>
  message tramNonSmpMsg {
    int next;
    T* payload_buffer;
  };

  template <typename T>
  group tramNonSmp {
    entry tramNonSmp();
    entry void insertValue(T const& value, int dest_pe);
    entry void tflush(); 
    entry void receive(tramNonSmpMsg<T>*);
  };
};
